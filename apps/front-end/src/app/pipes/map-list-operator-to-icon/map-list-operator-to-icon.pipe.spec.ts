import { MapListOperatorToIconPipe } from './map-list-operator-to-icon.pipe';

describe('MapListOperatorToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new MapListOperatorToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
