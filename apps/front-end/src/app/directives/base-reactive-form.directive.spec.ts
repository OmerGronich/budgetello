import { BaseReactiveFormDirective } from './base-reactive-form.directive';

describe('BaseReactiveFormDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseReactiveFormDirective();
    expect(directive).toBeTruthy();
  });
});
