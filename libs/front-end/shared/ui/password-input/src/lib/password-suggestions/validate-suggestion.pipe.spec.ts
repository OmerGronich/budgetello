import { ValidateSuggestionPipe } from './validate-suggestion.pipe';

describe('ValidateSuggestionPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateSuggestionPipe();
    expect(pipe).toBeTruthy();
  });
});
