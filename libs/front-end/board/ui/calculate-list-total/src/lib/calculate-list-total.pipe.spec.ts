import { CalculateListTotalPipe } from './calculate-list-total.pipe';

describe('CalculateListTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateListTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
